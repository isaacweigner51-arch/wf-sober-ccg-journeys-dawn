# v0.5.2 Leader Frame Fit Fix

- Home class portraits now use aspect-fit instead of aspect-cover, so faces, bodies, podiums, and backgrounds are not cut off.
- Removed the dark strip that overlapped leader artwork on the home screen.
- Added a dedicated square art viewport inside each leader card.
- Prebuilt deck previews now use a square full-art leader portrait rather than stretching square artwork into a tall slot.
- Profile avatar also uses aspect-fit.
- Existing dedicated Hope, Purpose, Serenity, and Courage art remains unchanged.
