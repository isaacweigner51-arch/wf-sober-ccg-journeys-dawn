# v0.7.2 Godot Type Cleanup

- Fixed the Godot 4.7 parser error at `main.gd:3821`.
- Explicitly typed the expanded attack target rectangle as `Rect2`.
- Explicitly typed the target-distance calculation as `float`.
- Preserved the v0.7.1 class-color compile hotfix and all v0.7.0 Sponsor cinematic changes.
- Updated Android version name/code and the in-project version manifest.

This is a parser/type cleanup build; no card balance or UI behavior was intentionally changed.
