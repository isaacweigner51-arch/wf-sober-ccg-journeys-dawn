# v0.7.1 Class Color Compile Hotfix

Fixed the GDScript parser error in `main.gd` at line 823.

- Replaced the nonexistent `class_color()` call with the existing `class_accent_color()` helper.
- Sponsor and signature evolution VFX now use the intended class accent color.
- No gameplay or balance changes were made in this hotfix.
