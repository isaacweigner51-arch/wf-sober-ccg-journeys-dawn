# v0.2.7 Type Inference Hotfix

- Fixed the parse error in `main.gd` around line 357.
- The Final Boss/meta deck fallback card now has an explicit `Dictionary` type.
- Added a safe empty-pool fallback instead of indexing an empty card list.
- Android version updated to 0.2.7 (code 27).
