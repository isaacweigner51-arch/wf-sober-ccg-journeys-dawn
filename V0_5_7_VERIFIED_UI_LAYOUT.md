# v0.5.7 Verified UI Layout

- Rebuilt Home screen into fixed non-overlapping regions.
- Rebuilt Battle Preparation into three reserved columns.
- Leader images use KEEP_ASPECT_CENTERED only.
- Removed leader images from narrow selector buttons.
- Owner-only deck controls remain role-gated.
- AI remains restricted to legal prebuilt decks.
- Set 16:9 aspect to keep so the fixed 1280x720 layout cannot stretch apart on different displays.
- Home music remains handled by AudioManager.
